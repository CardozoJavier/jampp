import styled from 'styled-components';
import { ButtonDropdownContainer } from './ButtonDropdownContainer.styles';

export const AccountDropdownContainer = styled(ButtonDropdownContainer)`
  z-index: 1;
`;
