import styled from 'styled-components';
import { OptionCheckboxGroup } from '../../OptionList/styles';

export const DropdownListContainer = styled.div`
  width: ${({ width }) => width || '100%'};
`;
